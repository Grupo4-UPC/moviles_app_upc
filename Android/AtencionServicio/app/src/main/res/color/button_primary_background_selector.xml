<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Enabled state -->
    <item android:color="@color/blue_500" android:state_enabled="true" />
    <!-- Disabled state -->
    <item android:color="@color/button_disabled_background_grey" android:state_enabled="false" />
    <!-- Default state (can be same as enabled or a different fallback) -->
    <item android:color="@color/blue_500" />
</selector>